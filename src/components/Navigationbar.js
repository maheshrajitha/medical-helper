import React from 'react';
import { Navbar } from 'react-bootstrap';
const Navigationbar = () => (
    <Navbar bg={'light'} variant={'light'}>
        <Navbar.Brand>Channel Doc</Navbar.Brand>
    </Navbar>
);

export default Navigationbar;
